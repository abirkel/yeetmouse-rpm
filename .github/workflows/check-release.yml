---
name: Check Upstream Versions

'on':
  schedule:
    # Build at 06:00 UTC every day (20 min after ublue images start building)
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  check_release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y skopeo jq

      - name: Load build config
        id: load_config
        run: |
          source build.conf
          echo "fedora_version=$CONTAINER_VERSION" >> "$GITHUB_OUTPUT"
          echo "Fedora version: $CONTAINER_VERSION"

      - name: Get latest YeetMouse commit
        id: get_yeetmouse
        run: |
          LATEST_COMMIT=$(gh api \
            repos/AndyFilter/YeetMouse/commits/master --jq '.sha')
          echo "latest_commit=$LATEST_COMMIT" >> "$GITHUB_OUTPUT"
          echo "Latest YeetMouse commit: $LATEST_COMMIT"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get Aurora kernel version
        id: get_aurora_kernel
        run: |
          chmod +x scripts/resolve-kernel-version.sh
          AURORA_KERNEL=$(
            ./scripts/resolve-kernel-version.sh --kernel-type main
          )
          echo "kernel_version=$AURORA_KERNEL" >> "$GITHUB_OUTPUT"
          echo "Aurora kernel version: $AURORA_KERNEL"

      - name: Get OGC kernel version
        id: get_ogc_kernel
        run: |
          OGC_KERNEL=$(
            ./scripts/resolve-kernel-version.sh \
              --kernel-type ogc \
              --fedora-version ${{ steps.load_config.outputs.fedora_version }}
          )
          echo "kernel_version=$OGC_KERNEL" >> "$GITHUB_OUTPUT"
          echo "OGC kernel version: $OGC_KERNEL"

      - name: Load current versions
        id: load_versions
        run: |
          if [ -f .external_versions ]; then
            source .external_versions
            echo "yeetmouse_commit=${YEETMOUSE_COMMIT:-}" \
              >> "$GITHUB_OUTPUT"
            echo "aurora_kernel_version=${AURORA_KERNEL_VERSION:-}" \
              >> "$GITHUB_OUTPUT"
            echo "ogc_kernel_version=${OGC_KERNEL_VERSION:-}" \
              >> "$GITHUB_OUTPUT"
          else
            echo "yeetmouse_commit=" >> "$GITHUB_OUTPUT"
            echo "aurora_kernel_version=" >> "$GITHUB_OUTPUT"
            echo "ogc_kernel_version=" >> "$GITHUB_OUTPUT"
          fi

      - name: Compare versions and determine build targets
        id: check_changes
        run: |
          LATEST_YEETMOUSE="${{ steps.get_yeetmouse.outputs.latest_commit }}"
          CURRENT_YEETMOUSE="${{
            steps.load_versions.outputs.yeetmouse_commit
          }}"

          LATEST_AURORA="${{
            steps.get_aurora_kernel.outputs.kernel_version
          }}"
          CURRENT_AURORA="${{
            steps.load_versions.outputs.aurora_kernel_version
          }}"

          LATEST_OGC="${{
            steps.get_ogc_kernel.outputs.kernel_version
          }}"
          CURRENT_OGC="${{
            steps.load_versions.outputs.ogc_kernel_version
          }}"

          echo "=== Version Comparison ==="
          echo "YeetMouse: $CURRENT_YEETMOUSE -> $LATEST_YEETMOUSE"
          echo "Aurora:    $CURRENT_AURORA -> $LATEST_AURORA"
          echo "OGC:       $CURRENT_OGC -> $LATEST_OGC"

          # Determine what changed
          YEETMOUSE_CHANGED=false
          AURORA_CHANGED=false
          OGC_CHANGED=false

          if [ "$LATEST_YEETMOUSE" != "$CURRENT_YEETMOUSE" ]; then
            YEETMOUSE_CHANGED=true
            echo "✓ YeetMouse commit changed"
          fi

          if [ "$LATEST_AURORA" != "$CURRENT_AURORA" ]; then
            AURORA_CHANGED=true
            echo "✓ Aurora kernel version changed"
          fi

          if [ "$LATEST_OGC" != "$CURRENT_OGC" ]; then
            OGC_CHANGED=true
            echo "✓ OGC kernel version changed"
          fi

          # Determine which kernel types need building
          KERNEL_TYPES=""
          BUILD_CLI=false
          NEEDS_BUILD=false

          if [ "$YEETMOUSE_CHANGED" = true ]; then
            # New YeetMouse commit: build for both kernel types
            KERNEL_TYPES="main,ogc"
            BUILD_CLI=true
            NEEDS_BUILD=true
            echo "→ Building for both kernel types (new YeetMouse commit)"
          else
            # Check individual kernel changes
            if [ "$AURORA_CHANGED" = true ]; then
              KERNEL_TYPES="main"
              NEEDS_BUILD=true
              echo "→ Building for main kernel (Aurora changed)"
            fi

            if [ "$OGC_CHANGED" = true ]; then
              if [ -n "$KERNEL_TYPES" ]; then
                KERNEL_TYPES="$KERNEL_TYPES,ogc"
              else
                KERNEL_TYPES="ogc"
              fi
              NEEDS_BUILD=true
              echo "→ Building for ogc kernel (OGC changed)"
            fi
          fi

          # Set outputs
          echo "needs_build=$NEEDS_BUILD" >> "$GITHUB_OUTPUT"
          echo "kernel_types=$KERNEL_TYPES" >> "$GITHUB_OUTPUT"
          echo "build_cli=$BUILD_CLI" >> "$GITHUB_OUTPUT"
          echo "yeetmouse_commit=$LATEST_YEETMOUSE" >> "$GITHUB_OUTPUT"
          echo "aurora_kernel=$LATEST_AURORA" >> "$GITHUB_OUTPUT"
          echo "ogc_kernel=$LATEST_OGC" >> "$GITHUB_OUTPUT"

          echo ""
          if [ "$NEEDS_BUILD" = true ]; then
            echo "=== Build Required ==="
            echo "Kernel types: $KERNEL_TYPES"
            echo "Build CLI: $BUILD_CLI"
            echo "YeetMouse commit: $LATEST_YEETMOUSE"
          else
            echo "→ No changes detected, no build needed"
          fi

      - name: Update versions file
        if: steps.check_changes.outputs.needs_build == 'true'
        run: |
          YEETMOUSE_COMMIT="${{
            steps.check_changes.outputs.yeetmouse_commit
          }}"
          AURORA_KERNEL_VERSION="${{
            steps.check_changes.outputs.aurora_kernel
          }}"
          OGC_KERNEL_VERSION="${{
            steps.check_changes.outputs.ogc_kernel
          }}"

          cat > .external_versions << EOF
          YEETMOUSE_COMMIT=$YEETMOUSE_COMMIT
          AURORA_KERNEL_VERSION=$AURORA_KERNEL_VERSION
          OGC_KERNEL_VERSION=$OGC_KERNEL_VERSION
          EOF

          git config user.name "github-actions[bot]"
          git config user.email \
            "github-actions[bot]@users.noreply.github.com"
          git add .external_versions
          COMMIT_MSG="chore: update versions"
          COMMIT_MSG="$COMMIT_MSG - yeetmouse: $YEETMOUSE_COMMIT"
          COMMIT_MSG="$COMMIT_MSG, aurora: $AURORA_KERNEL_VERSION"
          COMMIT_MSG="$COMMIT_MSG, ogc: $OGC_KERNEL_VERSION"
          git commit -m "$COMMIT_MSG" && git push

      - name: Trigger RPM build workflow
        if: steps.check_changes.outputs.needs_build == 'true'
        run: |
          YEETMOUSE_COMMIT="${{ steps.check_changes.outputs.yeetmouse_commit }}"
          KERNEL_TYPES="${{ steps.check_changes.outputs.kernel_types }}"
          BUILD_CLI="${{ steps.check_changes.outputs.build_cli }}"

          echo "Triggering build with:"
          echo "  yeetmouse_commit: $YEETMOUSE_COMMIT"
          echo "  kernel_types: $KERNEL_TYPES"
          echo "  build_cli: $BUILD_CLI"
          echo "  release_type: stable"

          gh workflow run build-rpm.yml --ref ${{ github.ref_name }} \
            -f yeetmouse_commit="$YEETMOUSE_COMMIT" \
            -f kernel_types="$KERNEL_TYPES" \
            -f build_cli="$BUILD_CLI" \
            -f release_type="stable"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
